import java.util.concurrent.Semaphore;

public class Main {

	public static void main(String[] args) {
		new Main();
	}

	public Main () {
		Semaphore s = new Semaphore(1);
		Window window = new Window(s);
		Physics p = new Physics(1000, s);
	}
	
}
